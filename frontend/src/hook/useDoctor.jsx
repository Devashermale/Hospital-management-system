import React from 'react'
import axios from 'axios'
function useDoctor() {

    const Doctor = async () => {
        try {
            const res = axios.post('',{
                
            })
        } catch (error) {
            
        }
    }
  return 
}

export default useDoctor